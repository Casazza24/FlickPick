# Local development defaults — copy this file to .env
# cp .env.example .env
# .env is gitignored and will never be committed

POSTGRES_DB=flickpick
POSTGRES_USER=flickpick
POSTGRES_PASSWORD=localdev

SESSION_SECRET=localdevsecret
